{
  "name": "test-tube-monorepo",
  "version": "0.0.1",
  "description": "Try something new here",
  "main": "./lib",
  "repository": "git@github.com:albburtsev/test-tube.git",
  "author": "Alexander Burtsev",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "format": "prettier --write 'packages/**/src/**/*.{ts,tsx}'",
    "uninstall": "rm -rf node_modules && rm -rf packages/*/node_modules",
    "reinstall": "yarn run uninstall && yarn"
  },
  "devDependencies": {
    "lerna": "^3.3.0",
    "prettier": "^1.14.2"
  }
}
